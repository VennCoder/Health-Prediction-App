If you are using an iOS device to run this app, download Expo Go from the App Store. Then, run this in your command prompt terminal:

npm install -g expo-cli

expo init HealthPredictionApp

cd HealthPredictionApp

npm install axios react-native-picker-select

After running the above commands, replace the files in your HealthPredictionApp directory with the ones in the repository.
